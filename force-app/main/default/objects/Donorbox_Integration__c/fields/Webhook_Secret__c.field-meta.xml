<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Webhook_Secret__c</fullName>
    <label>Webhook Secret</label>
    <length>255</length>
    <type>Text</type>
    <description>Shared secret expected in the X-Webhook-Secret header of incoming Donorbox webhook calls. Set the value manually in Setup (never via a deployed record) so it never lands in version control.</description>
</CustomField>

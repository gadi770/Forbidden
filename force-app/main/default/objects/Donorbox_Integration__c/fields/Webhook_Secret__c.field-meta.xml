<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Webhook_Secret__c</fullName>
    <label>Webhook Secret</label>
    <length>255</length>
    <type>Text</type>
    <description>Signing secret used to verify the HMAC signature Donorbox attaches to webhook deliveries (see DonorboxSignatureVerifier). Set the value manually in Setup (never via a deployed record) so it never lands in version control.</description>
</CustomField>
